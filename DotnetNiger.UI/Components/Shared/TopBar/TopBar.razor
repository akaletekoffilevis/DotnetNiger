@* Composant partagé : barre de navigation principale du site public (desktop + mobile). *@
@inject ISettingsService SettingsService
@inject IUploadService UploadService

<nav class="sticky z-50 w-full bg-background shadow-lg">
    <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
        <div class="flex h-16 justify-between">
            <!-- Logo / Marque -->
            <div class="flex items-center">
                <NavLink href="/" class="flex items-center space-x-2">
                    <img src="@resolvedLogoUrl" alt="@(settings?.LogoNom ?? "dotnet.niger")" class="h-9 w-9">
                </NavLink>
            </div>

            <!-- Navigation Desktop (à droite) -->
            <div class="hidden items-center space-x-6 md:flex">
                <NavLink href="/" Match="NavLinkMatch.All" ActiveClass="nav-active"
                         class="text-primary transition duration-150 ease-in-out hover:text-hover rounded-lg px-1 py-1">
                    <span class="text-sm font-normal">Accueil</span>
                </NavLink>
                <NavLink href="/propos" ActiveClass="nav-active"
                         class="text-primary transition duration-150 ease-in-out hover:text-hover rounded-lg px-1 py-1">
                    <span class="text-sm font-normal">À Propos</span>
                </NavLink>
                <NavLink href="/ressources" ActiveClass="nav-active"
                         class="text-primary transition duration-150 ease-in-out hover:text-hover rounded-lg px-1 py-1">
                    <span class="text-sm font-normal">Ressources</span>
                </NavLink>
                <NavLink href="/community" ActiveClass="nav-active"
                         class="text-primary transition duration-150 ease-in-out hover:text-hover rounded-lg px-1 py-1">
                    <span class="text-sm font-normal">Communauté</span>
                </NavLink>
                <NavLink href="/blog" ActiveClass="nav-active"
                         class="text-primary transition duration-150 ease-in-out hover:text-hover rounded-lg px-1 py-1">
                    <span class="text-sm font-normal">Blog</span>
                </NavLink>
                <NavLink href="/evenements" ActiveClass="nav-active"
                         class="text-primary transition duration-150 ease-in-out hover:text-hover rounded-lg px-1 py-1">
                    <span class="text-sm font-normal">Événements</span>
                </NavLink>
                <NavLink href="/partners" ActiveClass="nav-active"
                         class="text-primary transition duration-150 ease-in-out hover:text-hover rounded-lg px-1 py-1">
                    <span class="text-sm font-normal">Partenaires</span>
                </NavLink>
                <NavLink href="/contact" ActiveClass="nav-active"
                         class="text-primary transition duration-150 ease-in-out hover:text-hover rounded-lg px-1 py-1">
                    <span class="text-sm font-normal">Contact</span>
                </NavLink>

                <ThemeToggle />

                <TopBarAuthState />
            </div>

            <!-- Bouton menu mobile -->
            <div class="flex items-center gap-2 md:hidden">
                <TopBarAuthState Mobile="true" OnActionCompleted="OnAuthActionFromMobile" />
            </div>
        </div>
    </div>
</nav>

@code {
    private bool mobileMenuOpen = false;
    private PublicSettingsResponse? settings;
    private string resolvedLogoUrl = "/Logo.svg";

    protected override async Task OnInitializedAsync()
    {
        try
        {
            settings = await SettingsService.GetPublicSettingsAsync();
            if (settings is not null)
                resolvedLogoUrl = await UploadService.ResolveImageUrlAsync(settings.LogoUrl) ?? "/Logo.svg";
        }
        catch
        {
            // Fallback silencieux
        }
    }

    private void ToggleMobileMenu()
    {
        mobileMenuOpen = !mobileMenuOpen;
    }


    private void OnAuthActionFromMobile()
    {
        mobileMenuOpen = false;
    }
}