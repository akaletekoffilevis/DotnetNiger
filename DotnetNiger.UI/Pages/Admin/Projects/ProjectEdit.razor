@page "/admin/projects/edit/{Id:guid}"
@* @attribute [Authorize(Roles = "Admin,SuperAdmin,Collaborator")] *@
@* Page admin : édition d'un projet existant. *@
@using DotnetNiger.UI.Models.Requests
@using DotnetNiger.UI.Models.Responses
@inject NavigationManager Navigation
@inject IProjectService ProjectService
@inject IToastService ToastService

<div class="min-h-screen px-4 py-6 md:px-8 md:py-8">
    <div class="mx-auto max-w-4xl">
        <div class="mb-6 flex items-center justify-between gap-4">
            <AdminBreadcrumb />
            <button @onclick="GoBack"
                    class="inline-flex items-center gap-2 rounded-lg bg-red-500 px-4 py-2 text-sm font-semibold text-white transition hover:brightness-95">
                <i class="fas fa-arrow-left"></i>
                <span>Retour</span>
            </button>
        </div>

        <div class="rounded-lg bg-fondTransparent p-6 md:p-8">
            <h1 class="text-2xl font-bold text-title">Modifier le projet</h1>
            <p class="mt-1 text-sm text-muted">Modifiez les informations du projet.</p>

            @if (isLoading)
            {
                <div class="mt-8 space-y-6">
                    <Skeleton Type="SkeletonType.Title" Class="w-48" />
                    <Skeleton Type="SkeletonType.Text" Lines="1" Class="w-72" />
                    <div class="grid grid-cols-1 gap-6 md:grid-cols-2">
                        <div class="space-y-2">
                            <Skeleton Type="SkeletonType.Text" Lines="1" Class="w-16" />
                            <Skeleton Type="SkeletonType.Custom" Class="h-11 rounded-lg w-full" />
                        </div>
                        <div class="space-y-2">
                            <Skeleton Type="SkeletonType.Text" Lines="1" Class="w-16" />
                            <Skeleton Type="SkeletonType.Custom" Class="h-11 rounded-lg w-full" />
                        </div>
                    </div>
                    <div class="space-y-2">
                        <Skeleton Type="SkeletonType.Text" Lines="1" Class="w-24" />
                        <Skeleton Type="SkeletonType.Custom" Class="h-28 rounded-lg w-full" />
                    </div>
                    <div class="space-y-2">
                        <Skeleton Type="SkeletonType.Text" Lines="1" Class="w-20" />
                        <Skeleton Type="SkeletonType.Custom" Class="h-11 rounded-lg w-full" />
                    </div>
                    <div class="space-y-2">
                        <Skeleton Type="SkeletonType.Text" Lines="1" Class="w-24" />
                        <Skeleton Type="SkeletonType.Custom" Class="h-11 rounded-lg w-full" />
                    </div>
                    <div class="space-y-2">
                        <Skeleton Type="SkeletonType.Text" Lines="1" Class="w-24" />
                        <Skeleton Type="SkeletonType.Custom" Class="h-11 rounded-lg w-full" />
                    </div>
                    <div class="flex items-center gap-6">
                        <Skeleton Type="SkeletonType.Custom" Class="h-5 w-5 rounded" />
                        <Skeleton Type="SkeletonType.Custom" Class="h-5 w-5 rounded" />
                    </div>
                    <div class="flex gap-4">
                        <Skeleton Type="SkeletonType.Button" Width="140px" />
                        <Skeleton Type="SkeletonType.Button" Width="100px" />
                    </div>
                </div>
            }
            else if (notFound)
            {
                <div class="mt-8 rounded-lg bg-amber-50 px-4 py-6 text-center text-sm text-amber-700">
                    Projet introuvable.
                </div>
            }
            else
            {
                <EditForm Model="@updateRequest" OnValidSubmit="HandleSubmit" class="mt-8 space-y-6">
                    <DataAnnotationsValidator />

                    <div class="grid grid-cols-1 gap-6 md:grid-cols-2">
                        <div>
                            <label class="block text-sm font-semibold text-title">Titre *</label>
                            <InputText @bind-Value="updateRequest.Title"
                                       class="mt-1 w-full rounded-lg bg-surface shadow-md text-body px-4 py-3 text-sm transition focus:outline-none"
                                       placeholder="Nom du projet" />
                            <ValidationMessage For="@(() => updateRequest.Title)" class="mt-1 text-xs text-red-500" />
                        </div>

                        <div>
                            <label class="block text-sm font-semibold text-title">Statut</label>
                            <InputSelect @bind-Value="updateRequest.Status"
                                         class="mt-1 w-full rounded-lg shadow-md text-body bg-surface px-4 py-3 text-sm transition focus:outline-none">
                                <option value="active">Actif</option>
                                <option value="archived">Archivé</option>
                            </InputSelect>
                        </div>
                    </div>

                    <div>
                        <label class="block text-sm font-semibold text-title">Description *</label>
                        <InputTextArea @bind-Value="updateRequest.Description" rows="5"
                                       class="mt-1 w-full rounded-lg bg-surface shadow-md text-body px-4 py-3 text-sm transition focus:outline-none"
                                       placeholder="Décrivez le projet..." />
                        <ValidationMessage For="@(() => updateRequest.Description)" class="mt-1 text-xs text-red-500" />
                    </div>

                    <div>
                        <label class="block text-sm font-semibold text-title">URL GitHub</label>
                        <InputText @bind-Value="updateRequest.GithubUrl"
                                   class="mt-1 w-full rounded-lg bg-surface shadow-md text-body px-4 py-3 text-sm transition focus:outline-none"
                                   placeholder="https://github.com/..." />
                    </div>

                    <div>
                        <label class="block text-sm font-semibold text-title">Technologies</label>
                        <InputText @bind-Value="updateRequest.Technologies"
                                   class="mt-1 w-full rounded-lg bg-surface shadow-md text-body px-4 py-3 text-sm transition focus:outline-none"
                                   placeholder="ASP.NET Core, Blazor, PostgreSQL..." />
                    </div>

                    <div>
                        <label class="block text-sm font-semibold text-title">Image URL</label>
                        <InputText @bind-Value="updateRequest.ImageUrl"
                                   class="mt-1 w-full rounded-lg bg-surface shadow-md text-body px-4 py-3 text-sm transition focus:outline-none"
                                   placeholder="https://..." />
                    </div>

                    <div class="flex items-center gap-6">
                        <label class="flex items-center gap-2 text-sm text-title">
                            <InputCheckbox @bind-Value="isFeatured" />
                            Projet à la une
                        </label>
                        <label class="flex items-center gap-2 text-sm text-title">
                            <InputCheckbox @bind-Value="isPublished" />
                            Publié
                        </label>
                    </div>

                    @if (!string.IsNullOrWhiteSpace(errorMessage))
                    {
                        <div class="rounded-lg bg-red-50 px-4 py-3 text-sm text-red-600">@errorMessage</div>
                    }

                    <div class="flex gap-4">
                        <button type="submit"
                                class="inline-flex items-center gap-2 rounded-lg bg-primary px-6 py-3 text-sm font-semibold text-white transition hover:bg-blue-800">
                            <i class="fas fa-save"></i>
                            Enregistrer
                        </button>
                        <button type="button" @onclick="GoBack"
                                class="inline-flex items-center gap-2 rounded-lg bg-gray-200 px-6 py-3 text-sm font-semibold text-title transition hover:bg-gray-300">
                            Annuler
                        </button>
                    </div>
                </EditForm>
            }
        </div>
    </div>
</div>

@code {
    [Parameter] public Guid Id { get; set; }

    private UpdateProjectRequest updateRequest = new();
    private string errorMessage = string.Empty;
    private bool isLoading = true;
    private bool notFound;
    private bool isFeatured;
    private bool isPublished;
    private Guid _loadedId;

    protected override async Task OnInitializedAsync()
    {
        await LoadProjectAsync();
    }

    protected override async Task OnParametersSetAsync()
    {
        if (Id != Guid.Empty && _loadedId != Id)
        {
            await LoadProjectAsync();
        }
    }

    private async Task LoadProjectAsync()
    {
        isLoading = true;
        notFound = false;

        try
        {
            var project = await ProjectService.GetByIdAsync(Id);
            if (project is null)
            {
                notFound = true;
                return;
            }

            updateRequest = new UpdateProjectRequest
            {
                Title = project.Title,
                Description = project.Description,
                GithubUrl = project.GithubUrl,
                ImageUrl = project.ImageUrl,
                Technologies = project.Technologies,
                Status = project.Status,
                IsFeatured = project.IsFeatured,
                IsPublished = project.IsPublished
            };
            isFeatured = project.IsFeatured;
            isPublished = project.IsPublished;
        }
        catch
        {
            notFound = true;
        }
        finally
        {
            _loadedId = Id;
            isLoading = false;
        }
    }

    private void GoBack()
    {
        Navigation.NavigateTo("/admin/projects");
    }

    private async Task HandleSubmit()
    {
        errorMessage = string.Empty;

        try
        {
            updateRequest.IsFeatured = isFeatured;
            updateRequest.IsPublished = isPublished;
            var updated = await ProjectService.UpdateAsync(Id, updateRequest);
            if (updated is null)
            {
                errorMessage = "Impossible de mettre à jour le projet.";
                ToastService.ShowToast("Erreur lors de la mise à jour.", ToastLevel.Error);
                return;
            }

            ToastService.ShowToast("Projet mis à jour avec succès.", ToastLevel.Success);
            Navigation.NavigateTo("/admin/projects");
        }
        catch (Exception ex)
        {
            errorMessage = ex.Message;
            ToastService.ShowToast("Erreur lors de la mise à jour du projet.", ToastLevel.Error);
        }
    }
}
